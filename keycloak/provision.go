package keycloak

import "context"

func Provision(ctx context.Context, addr, adminClientID, adminClientSecret string) error {
	// create the realms club and players
	// create two clients
	return nil
}
